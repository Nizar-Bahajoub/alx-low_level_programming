#include <stdio.h>

/** 
 * main - printing the alphabet
 *
 * Return: 0
 */

int main(void)
{
	char i = 'a';
	while (i <= 'z' && i != 'q' && i !='e')
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
